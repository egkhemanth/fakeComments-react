import React from 'react'
import ReactDOM from 'react-dom'

const App =(props)=>{
   
        return(
            <div>
            <h1>{props.name} </h1>
        
        </div>
    );
    };
    export default App;
